import { ServerSettings } from "server_settings.slint";
import { ServerState } from "server_state.slint";

export struct ServerProfile {
    id: string,
    name: string,
    settings: ServerSettings,
    state: ServerState
}