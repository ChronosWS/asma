export global GlobalConfiguration {
    in-out property<string> version;
    in-out property<string> local_ip;
    in-out property<string> app_data_directory;
    in-out property<string> profiles_directory;
    in-out property<string> steamcmd_directory;
}
