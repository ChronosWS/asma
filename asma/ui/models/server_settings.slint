// ServerSettings
//
// The design-time values for the server.  These values get saved to the profile and are used
// to configure the game server
export struct ServerSettings {
    installation_location: string,
}