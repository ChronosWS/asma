// ServerState
//
// This is the runtime state of the server.  These values do not get saved anywhere
export struct ServerState {
    installed_version: string,
    status: string,
    availability: string,
    current_players: int,
    max_players: int
}